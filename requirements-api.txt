# Inference-only runtime dependencies for the Docker image.
# Excludes training (datasets, scikit-learn), evaluation, and notebook packages.
# Use requirements.txt for a full local development environment.

# ML inference
torch>=2.2.0
transformers>=4.40.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.1.0
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=8.0.0
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
celery[redis]>=5.3.0
